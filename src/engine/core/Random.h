#pragma once

class Random
{
public:
    // number in range [start, end]
    static int getRandomInt(int start, int end);
    static unsigned int getRandomUInt(unsigned int start, unsigned int end);
};
